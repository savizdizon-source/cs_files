Selection Sort Algorithm

The first step was to find the smallest value in the given array and put that first (swapping the value of the original first and the new smallest value)

From there, move to the second element and check if there's a smaller value in the rest of the array. 
    If there is, swap those two values.

The time complexity for this is O(n^2) because of the nested for loop.

