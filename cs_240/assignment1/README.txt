1. 
Takes input from user and uses ord function to print the ASCII value of each letter

2.


3. 
Takes an image file and reads each pixel's color then uses that 
hexcode to return a character (i.e R for red) in the format of the image
def convert(color) takes the hexcode and returns a string

4.
Goes from the character to the hexcode using convert(color)